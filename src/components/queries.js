const faqs = [
  {
    query: "Can one connect more than one device ?",
    Answer:
      "Yes It uses a wide area network(WAN) thus it is possible for it to connect to more than one.",
  },

];

export default faqs;
